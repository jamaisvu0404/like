# プライズフィギュア カレンダー自動生成手順（AI用指示書・完全保存版）

ユーザーから特定のURL（例: `https://tokutame.net/prize-matome-YYYYMM/`）を指定して「カレンダーレイアウトを作って」「更新して」といった依頼があった場合、以下の手順で処理を実行してください。

---

## 1. 実行コマンド（これだけで全自動完結）

指定されたURLを引数にして、以下のスクリプトを実行してください。
```bash
python "h:\マイドライブ\python\プライズフィギュア\web情報まとめ\auto_generate_calendar.py" <指定されたURL>
```

---

## 2. スクリプトの動作内容（全自動処理の流れ）

1. **データ収集＆キャッシュ**:
   - 引数で渡されたURLから「年月」を自動抽出（例: `202608`）。
   - 同じフォルダ内にその年月（例: `202608/`）のフォルダを自動作成。
   - サイトのHTMLから対象フィギュアの情報を抽出し、画像を自動的にダウンロード・保存。
2. **カレンダーHTML生成**:
   - 後述の必須仕様・デザイン要件をすべて満たした `calendar.html` を生成。
3. **GitHub自動アップロード・デプロイ**:
   - 生成完了直後に自動で `C:\Users\pande\prize-calendar` を経由し、GitHubリポジトリ（`jamaisvu0404/like`）へ `git add` ＞ `git commit` ＞ `git push` を実行。
   - スマホ用Webサイト（`https://jamaisvu0404.github.io/like/`）が**全自動で最新状態に更新**されます。

---

## 3. カレンダーHTMLの必須仕様・ルール（全て記憶・遵守すること）

### ① デザインテーマ（完全ライトモード）
- **完全ライトモード（Light Mode）**:
  - 全体の背景色は明るいライトグレー/オフホワイト（`#f1f5f9` / `#f8fafc`）。
  - カードやコンテナは清潔感のある純白（`#ffffff`）、読みやすい濃色テキスト（`#0f172a`）。
  - ヘッダーは鮮やかなグラデーション（`#e63946` 〜 `#ba181b`）。

### ② スマホ専用最適化（3列表示＆列数切替）
- スマホ画面（幅650px以下）では標準で **3列レイアウト**（1画面に6〜9個以上の商品が一覧できるコンパクト設計）。
- ツールバーに **`[2列] [3列] [4列]`** の切り替えボタンを設置（選択した列数はlocalStorageに保存）。
- 日付セル、余白、フォントサイズをスマホ用にスリム化。

### ③ メルカリ検索最適化キーワード生成
- **英語キャラ名のカナ変換**: `MONKEY.D.LUFFY GEAR5` → `モンキー D ルフィ ギア5`、`ZARAKI KENPACHI` → `更木剣八` 等。
- **作品名のスマート付与**: 「くらげ」「ハム」等で埋もれないよう、先頭に作品名（初音ミク、トイストーリー、ワンピース等）をスマート付与。
- **不要な英語シリーズ名の除外**: `HUNTING ARCHIVES`, `BANPRESTO EVOLVE`, `THE AMAZING HEROES`, `Classical tuning` 等のノイズ英語をカット。
- **記号の完全排除**: `・`, `.`, `-`, `~`, `“`, `”`, `‼`, `!` 等の全記号をスペース置換。

### ④ メルカリ直接検索ボタン
- 各カードに目立つ「**🔴 メルカリ**」ボタンを配置。
- クリックすると別タブでメルカリが直接開く。
  - **販売状況**: **売り切れ (`status=sold_out`)**
  - **並び順**: **新しい順 (`sort=created_time&order=desc`)**
- ボタン横の「**📋**」でキーワードコピーも可能。

### ⑤ 末尾追加キーワード機能（箱無しボタン）
- 上部ツールバーで「箱無し」「未開封」などの末尾キーワード付与を管理（自由編集可能、localStorageに自動保存）。

### ⑥ カードクリック動作モード切替
- ツールバー上で「🔴 メルカリ検索」と「📋 名前コピー」のどちらでも自由に切り替え可能。

### ⑦ 画像保存機能
- 右上の「📷 画像で保存」ボタンで、カレンダー全体を高解像度PNGとしてダウンロード可能。

---

## 4. ユーザーへの報告
スクリプトの実行が正常に完了したら、ユーザーに以下のことを報告してください。
- カレンダーHTMLの生成および **GitHub（Webサイト）への自動アップロードが完了したこと**。
- 公開URL（`https://jamaisvu0404.github.io/like/`）を案内すること。
