# プライズフィギュア カレンダー自動生成手順（AI用指示書）

ユーザーから特定のURL（例: `https://tokutame.net/prize-matome-YYYYMM/`）を指定して「カレンダーレイアウトを作って」「更新して」といった依頼があった場合、以下の手順で処理を実行してください。

## 実行手順

1. **スクリプトの実行**
   指定されたURLを引数にして、以下のスクリプトを実行してください。
   ```bash
   python "h:\マイドライブ\python\プライズフィギュア\web情報まとめ\auto_generate_calendar.py" <指定されたURL>
   ```

2. **スクリプトの動作内容（全自動完結）**
   - 引数で渡されたURLから「年月」を自動抽出します（例: `202608`）。
   - 同じフォルダ内にその年月（例: `202608/`）のフォルダを自動作成します。
   - サイトのHTMLから対象フィギュアの情報を抽出し、画像を自動的にローカルへキャッシュ・保存します。
   - カード型グリッドレイアウトのモダンなカレンダーHTML（`calendar.html`）を生成します。
   - **【最重要】GitHub自動アップロード・デプロイ**:
     - スクリプト実行完了時に、自動的に `C:\Users\pande\prize-calendar` を経由して GitHub リポジトリ（`jamaisvu0404/like`）へ最新ファイルを `git add` & `git commit` & `git push` します。
     - これにより、スマホ用Webサイト（`https://jamaisvu0404.github.io/like/`）が**全自動で最新状態に更新**されます。

3. **カレンダーHTMLの必須仕様・機能**
   - **スマホ専用最適化（3列表示＆列数切替）**:
     - スマホ表示時は標準で **3列レイアウト** になり、1画面で6〜9個以上の商品が見渡せるコンパクト設計。
     - 上部ツールバーに `[2列] [3列] [4列]` の切り替えボタンを設置（localStorageに保存）。
   - **メルカリ検索最適化キーワード生成**:
     - キャラクター名・形態名の英語表記は日本語/カタカナに自動変換（例: `MONKEY.D.LUFFY GEAR5` → `モンキー D ルフィ ギア5`）。
     - 一般的な単語（くらげ、ハム等）で埋もれないよう、作品名（初音ミク、トイストーリー等）をスマートに先頭付与。
     - 検索ヒットを阻害する長すぎるマイナー英語シリーズ名（`HUNTING ARCHIVES`, `BANPRESTO EVOLVE` 等）は自動除外。
     - 「・」「.」「-」「~」「感嘆符」「引用符」「括弧」等の**あらゆる記号は完全にスペース化（除去）**。
   - **メルカリ直接検索機能**:
     - 各カードの「🔴 メルカリ」ボタンを押すと、別タブでメルカリが直接開く。
     - 検索条件: **売り切れ (`status=sold_out`)** ＋ **新しい順 (`sort=created_time&order=desc`)**。
   - **末尾追加キーワード（箱無しボタン）**:
     - 上部ツールバーで「箱無し」「未開封」などの末尾キーワード付与を管理（自由編集可能、localStorageに自動保存）。
   - **カードクリック動作モード切替**:
     - ツールバー上で「🔴 メルカリ検索」と「📋 名前コピー」を切り替え可能。
   - **画像保存機能**:
     - 右上の「画像で保存」ボタンで、カレンダー全体を高解像度PNGとしてダウンロード可能。

4. **ユーザーへの報告**
   スクリプトの実行が正常に完了したら、ユーザーに以下のことを報告してください。
   - カレンダーHTMLの生成および **GitHub（Webサイト）への自動アップロードが完了したこと**。
   - 公開URL（`https://jamaisvu0404.github.io/like/`）を案内すること。
