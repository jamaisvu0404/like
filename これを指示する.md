# プライズ重心情報データベース 自動更新＆GitHub公開 指示書（AI用・完全保存版）

ユーザーから「重心情報を更新して」「最新の重心データを反映して」「これを指示する.md」などの指示があった場合、AIは以下の手順を実行して**データ収集からGitHub公開までを全自動で完結**させてください。

---

## 1. 実行コマンド（これだけで全自動完結）

以下のコマンドをターミナルで実行してください。

```bash
python -X utf8 "h:\マイドライブ\python\プライズフィギュア\web情報まとめ\fetch_merry_gravity.py" 3500
```

その後、以下のPythonコードを実行して各作業フォルダおよび GitHub（`https://github.com/jamaisvu0404/like.git`）へ同期・プッシュしてください。

```python
import os, shutil, subprocess

src_dir = r"h:\マイドライブ\python\プライズフィギュア\web情報まとめ"
gravity_dir = r"h:\マイドライブ\python\プライズフィギュア\重心情報"
dst_dir = r"C:\Users\pande\prize-calendar"

# 同期対象ファイル一覧
files = [
    'index.html', 'gravity.html', 'prize_gravity_viewer.html', 
    'gravity_data.json', 'gravity_sync_info.json', 
    'fetch_merry_gravity.py', 'これを指示する.md'
]

# 1. 重心情報フォルダへの同期
for f in files:
    s = os.path.join(src_dir, f)
    d = os.path.join(gravity_dir, f)
    if os.path.exists(s) and f != 'index.html':
        shutil.copy2(s, d)

# 2. prize-calendar (GitHubリポジトリ用フォルダ) への同期
for f in files:
    s = os.path.join(src_dir, f)
    d = os.path.join(dst_dir, f)
    if os.path.exists(s):
        shutil.copy2(s, d)

# 3. Git Commit & Push
subprocess.run(['git', 'add', '.'], cwd=dst_dir, check=True)
subprocess.run(['git', 'commit', '-m', 'Auto update prize gravity data (2026)'], cwd=dst_dir)
subprocess.run(['git', 'push', 'origin', 'main'], cwd=dst_dir, check=True)
```

---

## 2. 収集対象とシステム仕様

### ① 収集対象アカウント（2026年分のみ）
- **Merry☆An（みりあん）** (`@6eS8Jm4YNJpPA2D`)
- **831生活** (`@831suky`)
- **あかり** (`@yohane150`)
- **もぐらクレーン** (`@mogurakurenn`)
※ 2026年（`2026-01-01` 以降）に投稿されたポストのみを自動抽出してデータベースに反映します。
※ 既存の `gravity_data.json` にあるデータはキャッシュされ、前回更新以降の新規投稿のみを差分取得します。

### ② 生成・更新されるファイル
- `gravity.html`: 重心情報ビューア（GitHub Pages 公開用）
- `gravity_data.json`: 構造化された重心データキャッシュ
- `gravity_sync_info.json`: 前回更新日時・差分追加数・履歴メタデータ
- `index.html`: 入荷カレンダー＆全月横断検索（ヘッダーに重心情報への相互リンクあり）

### ③ 公開先URL
- **重心情報データベース**: `https://jamaisvu0404.github.io/like/gravity.html`
- **入荷カレンダー＆横断検索**: `https://jamaisvu0404.github.io/like/`

---

## 3. ユーザーへの完了報告フォーマット

処理完了後、ユーザーに以下の内容を簡潔に報告してください。
1. **更新日時情報**: 前回更新日時（例: 2026-08-23 09:17）および 今回更新日時
2. **新規追加件数＆累計件数**: （例: 今回新規 +〇件 / 2026年累計 計〇〇件）
3. **投稿者別内訳**: 各投稿者の累計件数および新規追加件数
4. **GitHub Pages への公開完了**
5. **アクセスURL**:
   - 重心情報: `https://jamaisvu0404.github.io/like/gravity.html`
   - 入荷カレンダー: `https://jamaisvu0404.github.io/like/`
